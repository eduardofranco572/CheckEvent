services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
  rabbitmq:
    image: rabbitmq:3-management-alpine
    ports:
      - "5672:5672" # Porta de comunicação
      - "15672:15672" # Painel visual (http://localhost:15672)
